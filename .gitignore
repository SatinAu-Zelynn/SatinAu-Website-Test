# ==============================
# 通用忽略规则（所有项目适用）
# ==============================
# 操作系统生成的文件
.DS_Store               # Mac系统文件
Thumbs.db               # Windows缩略图缓存
desktop.ini             # Windows文件夹配置文件
*.swp                   # Vim临时文件
*.swo                   # Vim交换文件
*~                      # 临时备份文件（如Linux下的文件编辑备份）

# 日志文件
npm-debug.log*          # npm错误日志
yarn-error.log*         # yarn错误日志
pnpm-debug.log*         # pnpm错误日志
lerna-debug.log*        # Lerna日志

# 缓存目录
.node_cache/            # Node缓存
.cache/                 # 通用缓存（如React构建缓存）
.eslintcache            # ESLint缓存


# ==============================
# React项目专属忽略规则
# ==============================
# 依赖包依赖目录（体积大，可通过npm install重建）
node_modules/

# 构建产物目录（部署时自动生成，无需提交）
build/                  # create-react-app构建目录
dist/                   # 其他构建工具（如Vite）的产物目录

# 环境变量文件（可能包含敏感信息，如API密钥）
.env                    # 通用环境变量
.env.local              # 本地环境变量（优先级最高，不提交）
.env.development        # 开发环境变量（如需共享可删除此条）
.env.production         # 生产环境变量（如需共享可删除此条）
.env.test               # 测试环境变量
.env*.local             # 所有带.local后缀的环境变量文件

# 测试相关目录/文件
coverage/               # 测试覆盖率报告目录
*.test.js.snap          # Jest测试快照文件

# 打包配置文件（如使用自定义打包配置，非必要不提交）
webpack.config.js       # 如需共享可删除此条
vite.config.js          # 如需共享可删除此条


# ==============================
# IDE/编辑器配置（按个人需求保留）
# ==============================
# VS Code配置
.vscode/
!.vscode/extensions.json  # 如需共享扩展推荐列表，保留此条（前面加!表示不忽略）
!.vscode/settings.json    # 如需共享编辑器设置，保留此条
!.vscode/launch.json      # 如需共享调试配置，保留此条

# WebStorm/IntelliJ IDEA配置
.idea/
*.iml
*.iws
*.ipr

# Sublime Text配置
.sublime-project
.sublime-workspace

# Atom配置
.atom/

# Visual Studio配置
.vs/


# ==============================
# 其他特殊文件（根据项目需求调整）
# ==============================
# 临时文件目录
tmp/
temp/

# 备份文件
*.bak
*.tmp
*.old

# 大型静态资源（如本地字体、视频，建议通过CDN引入）
# 注：若项目中保留了本地字体（如MiSans），需删除以下行，或仅排除非必要资源
src/source/font/        # 本地字体目录（如需提交字体，删除此条）
src/source/video/       # 本地视频目录
